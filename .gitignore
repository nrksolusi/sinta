# Dependencies
node_modules/

# Build output
dist/
client/.tanstack/
client/.nitro/
client/.output/
server/bin/

# dev.sh runtime (pids, logs)
.run/

# Environment - secrets never enter git (CONVENTIONS.md)
.env
.env.*
# ...but the committed template is safe (placeholders only).
!.env.example
!*/.env.example

# Claude Code personal overrides
.claude/settings.local.json

# OS / editor
.DS_Store

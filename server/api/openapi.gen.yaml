# GENERATED by cmd/bundle - DO NOT EDIT.
# Sources: api/openapi.yaml + api/paths/*.yaml
# Edit those, then run `go generate ./...` from server/.

components:
    parameters:
        ProductFilter:
            in: query
            name: productId
            required: false
            schema:
                format: uuid
                type: string
        WarehouseFilter:
            in: query
            name: warehouseId
            required: false
            schema:
                format: uuid
                type: string
    responses:
        Conflict:
            content:
                application/json:
                    schema:
                        $ref: '#/components/schemas/Error'
            description: Resource already exists
        Forbidden:
            content:
                application/json:
                    schema:
                        $ref: '#/components/schemas/Error'
            description: Authenticated but not permitted
        Unauthorized:
            content:
                application/json:
                    schema:
                        $ref: '#/components/schemas/Error'
            description: Not authenticated
        ValidationError:
            content:
                application/json:
                    schema:
                        $ref: '#/components/schemas/Error'
            description: Request failed validation
    schemas:
        CostingMethod:
            description: Locked at onboarding; switchable only at a fiscal year boundary (ADR-0002)
            enum:
                - weightedAverage
                - fifo
            type: string
        CreateInviteRequest:
            properties:
                role:
                    $ref: '#/components/schemas/Role'
            required:
                - role
            type: object
        CreateTenantRequest:
            properties:
                costingMethod:
                    $ref: '#/components/schemas/CostingMethod'
                fiscalYearStartMonth:
                    default: 1
                    maximum: 12
                    minimum: 1
                    type: integer
                legalName:
                    type: string
                name:
                    minLength: 1
                    type: string
                warehouse:
                    $ref: '#/components/schemas/WarehouseInput'
            required:
                - name
                - costingMethod
                - warehouse
            type: object
        Error:
            properties:
                code:
                    type: string
                fields:
                    additionalProperties:
                        type: string
                    description: Per-field validation messages, when applicable
                    type: object
                message:
                    type: string
            required:
                - code
                - message
            type: object
        Health:
            properties:
                status:
                    enum:
                        - ok
                    type: string
            required:
                - status
            type: object
        Invite:
            properties:
                expiresAt:
                    format: date-time
                    type: string
                id:
                    format: uuid
                    type: string
                role:
                    $ref: '#/components/schemas/Role'
                token:
                    type: string
            required:
                - id
                - role
                - token
                - expiresAt
            type: object
        InvitePublic:
            properties:
                role:
                    $ref: '#/components/schemas/Role'
                tenantName:
                    type: string
            required:
                - tenantName
                - role
            type: object
        LoginRequest:
            properties:
                email:
                    format: email
                    type: string
                password:
                    format: password
                    type: string
            required:
                - email
                - password
            type: object
        Member:
            properties:
                email:
                    format: email
                    type: string
                name:
                    type: string
                role:
                    $ref: '#/components/schemas/Role'
                userId:
                    format: uuid
                    type: string
            required:
                - userId
                - name
                - email
                - role
            type: object
        Membership:
            properties:
                role:
                    $ref: '#/components/schemas/Role'
                tenant:
                    $ref: '#/components/schemas/Tenant'
            required:
                - tenant
                - role
            type: object
        RegisterRequest:
            properties:
                email:
                    format: email
                    type: string
                name:
                    minLength: 1
                    type: string
                password:
                    format: password
                    minLength: 10
                    type: string
            required:
                - email
                - password
                - name
            type: object
        Role:
            enum:
                - owner
                - admin
                - warehouse
                - sales
                - viewer
            type: string
        SessionInfo:
            properties:
                activeTenantId:
                    description: Absent until a tenant is selected
                    format: uuid
                    type: string
                memberships:
                    items:
                        $ref: '#/components/schemas/Membership'
                    type: array
                user:
                    $ref: '#/components/schemas/User'
            required:
                - user
                - memberships
            type: object
        StockCardEntry:
            properties:
                batchId:
                    description: Absent for stock that is not batch-tracked
                    format: uuid
                    type: string
                docId:
                    format: uuid
                    type: string
                docType:
                    type: string
                effectiveAt:
                    format: date-time
                    type: string
                movementId:
                    format: uuid
                    type: string
                movementType:
                    enum:
                        - receipt
                        - issue
                        - transfer_in
                        - transfer_out
                        - adjustment
                        - opname
                        - cost_correction
                        - revaluation
                    type: string
                provisional:
                    description: True when the movement was valued below zero stock (D6)
                    type: boolean
                qty:
                    description: Signed movement quantity in base units, as a decimal string
                    type: string
                runningQty:
                    description: On-hand quantity after this movement, as a decimal string
                    type: string
                runningValue:
                    description: Total stock value after this movement, as a decimal string
                    type: string
                seq:
                    format: int64
                    type: integer
                unitCost:
                    description: Cost booked for this movement by the costing engine, as a decimal string
                    type: string
                warehouseId:
                    format: uuid
                    type: string
            required:
                - movementId
                - warehouseId
                - movementType
                - qty
                - unitCost
                - effectiveAt
                - seq
                - runningQty
                - runningValue
                - provisional
            type: object
        StockCardReport:
            properties:
                entries:
                    items:
                        $ref: '#/components/schemas/StockCardEntry'
                    type: array
                productId:
                    format: uuid
                    type: string
            required:
                - productId
                - entries
            type: object
        StockOnHandReport:
            properties:
                rows:
                    items:
                        $ref: '#/components/schemas/StockOnHandRow'
                    type: array
            required:
                - rows
            type: object
        StockOnHandRow:
            properties:
                batchId:
                    description: Absent for stock that is not batch-tracked
                    format: uuid
                    type: string
                batchNo:
                    type: string
                productId:
                    format: uuid
                    type: string
                productName:
                    type: string
                qtyOnHand:
                    description: On-hand quantity in base units, as a decimal string
                    type: string
                sku:
                    type: string
                warehouseCode:
                    type: string
                warehouseId:
                    format: uuid
                    type: string
                warehouseName:
                    type: string
            required:
                - productId
                - sku
                - productName
                - warehouseId
                - warehouseCode
                - warehouseName
                - qtyOnHand
            type: object
        StockValuationReport:
            properties:
                rows:
                    items:
                        $ref: '#/components/schemas/StockValuationRow'
                    type: array
                totalValue:
                    description: Sum of value across all rows, as a decimal string
                    type: string
            required:
                - rows
                - totalValue
            type: object
        StockValuationRow:
            properties:
                avgCost:
                    description: Weighted-average unit cost, as a decimal string
                    type: string
                batchId:
                    description: Absent for stock that is not batch-tracked
                    format: uuid
                    type: string
                batchNo:
                    type: string
                productId:
                    format: uuid
                    type: string
                productName:
                    type: string
                qtyOnHand:
                    description: On-hand quantity in base units, as a decimal string
                    type: string
                sku:
                    type: string
                value:
                    description: qtyOnHand * avgCost, as a decimal string
                    type: string
                warehouseCode:
                    type: string
                warehouseId:
                    format: uuid
                    type: string
                warehouseName:
                    type: string
            required:
                - productId
                - sku
                - productName
                - warehouseId
                - warehouseCode
                - warehouseName
                - qtyOnHand
                - avgCost
                - value
            type: object
        SwitchTenantRequest:
            properties:
                tenantId:
                    format: uuid
                    type: string
            required:
                - tenantId
            type: object
        Tenant:
            properties:
                id:
                    format: uuid
                    type: string
                name:
                    type: string
            required:
                - id
                - name
            type: object
        TenantProfile:
            properties:
                costingMethod:
                    $ref: '#/components/schemas/CostingMethod'
                fiscalYearStartMonth:
                    type: integer
                id:
                    format: uuid
                    type: string
                legalName:
                    type: string
                myRole:
                    $ref: '#/components/schemas/Role'
                name:
                    type: string
            required:
                - id
                - name
                - legalName
                - costingMethod
                - fiscalYearStartMonth
                - myRole
            type: object
        UpdateMemberRequest:
            properties:
                role:
                    $ref: '#/components/schemas/Role'
            required:
                - role
            type: object
        UpdateTenantRequest:
            properties:
                fiscalYearStartMonth:
                    maximum: 12
                    minimum: 1
                    type: integer
                legalName:
                    type: string
                name:
                    minLength: 1
                    type: string
            type: object
        User:
            properties:
                email:
                    format: email
                    type: string
                id:
                    format: uuid
                    type: string
                name:
                    type: string
            required:
                - id
                - email
                - name
            type: object
        WarehouseInput:
            properties:
                code:
                    minLength: 1
                    type: string
                name:
                    minLength: 1
                    type: string
            required:
                - code
                - name
            type: object
    securitySchemes:
        sessionCookie:
            in: cookie
            name: sinta_session
            type: apiKey
info:
    description: |
        Multi-tenant inventory management for Indonesian SME distributors. Contract-first: this file is the single source of truth for both the Go server (oapi-codegen) and the TypeScript client (openapi-typescript). Conventions: camelCase fields, kebab-case plural paths, /v1 prefix (docs/CONVENTIONS.md).
    title: Sinta API
    version: 0.1.0
openapi: 3.0.3
paths:
    /auth/login:
        post:
            operationId: login
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/LoginRequest'
                required: true
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/SessionInfo'
                    description: Session started (session cookie set)
                "401":
                    $ref: '#/components/responses/Unauthorized'
            security: []
            summary: Authenticate and start a session
    /auth/logout:
        post:
            operationId: logout
            responses:
                "204":
                    description: Session ended
                "401":
                    $ref: '#/components/responses/Unauthorized'
            summary: End the current session
    /auth/register:
        post:
            operationId: register
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/RegisterRequest'
                required: true
            responses:
                "201":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/User'
                    description: Account created
                "409":
                    $ref: '#/components/responses/Conflict'
                "422":
                    $ref: '#/components/responses/ValidationError'
            security: []
            summary: Create a user account
    /auth/session:
        get:
            operationId: getSession
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/SessionInfo'
                    description: Current session
                "401":
                    $ref: '#/components/responses/Unauthorized'
            summary: Current user, memberships, and active tenant
    /auth/switch-tenant:
        post:
            operationId: switchTenant
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/SwitchTenantRequest'
                required: true
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/SessionInfo'
                    description: Active tenant changed
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    $ref: '#/components/responses/Forbidden'
            summary: Change the session's active tenant
    /health:
        get:
            operationId: getHealth
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Health'
                    description: Service is up
            security: []
            summary: Liveness check
    /invites/{token}:
        get:
            operationId: getInvite
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/InvitePublic'
                    description: Invite details
                "404":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Unknown or revoked invite
                "410":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Invite expired
            security: []
            summary: Public invite details for the landing page
        parameters:
            - in: path
              name: token
              required: true
              schema:
                type: string
    /invites/{token}/accept:
        parameters:
            - in: path
              name: token
              required: true
              schema:
                type: string
        post:
            operationId: acceptInvite
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/SessionInfo'
                    description: Joined; tenant activated for this session
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "404":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Unknown or revoked invite
                "410":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
                    description: Invite expired
            summary: Join the inviting tenant with the invite's role
    /reports/stock-card:
        get:
            description: |
                The ordered journal for one product (optionally one warehouse), each movement carrying the running on-hand quantity, booked unit cost, and running value the tenant's costing engine derives at that point. This is the stock audit trail; it is never rewritten (ADR-0001).
            operationId: reportStockCard
            parameters:
                - in: query
                  name: productId
                  required: true
                  schema:
                    format: uuid
                    type: string
                - $ref: '#/components/parameters/WarehouseFilter'
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/StockCardReport'
                    description: Movement history for the product
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    $ref: '#/components/responses/Forbidden'
                "422":
                    $ref: '#/components/responses/ValidationError'
            summary: Per-product movement history with a running balance
    /reports/stock-on-hand:
        get:
            description: |
                The derived stock level for every (product, warehouse, batch) key with a non-zero position, read from the stock_levels cache. Filterable by warehouse and/or product.
            operationId: reportStockOnHand
            parameters:
                - $ref: '#/components/parameters/WarehouseFilter'
                - $ref: '#/components/parameters/ProductFilter'
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/StockOnHandReport'
                    description: On-hand rows for the active tenant
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    $ref: '#/components/responses/Forbidden'
            summary: Current on-hand quantity per product, warehouse, and batch
    /reports/stock-valuation:
        get:
            description: |
                The value of stock on hand per (product, warehouse, batch) key, computed as qty_on_hand * avg_cost from the stock_levels cache under the tenant's costing method (weighted average in M1). The cache is a faithful reduction of the journal maintained transactionally at posting, so this equals folding the journal through the costing engine.
            operationId: reportStockValuation
            parameters:
                - $ref: '#/components/parameters/WarehouseFilter'
                - $ref: '#/components/parameters/ProductFilter'
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/StockValuationReport'
                    description: Valuation rows for the active tenant
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    $ref: '#/components/responses/Forbidden'
            summary: Monetary value of stock per product and warehouse
    /tenant:
        get:
            operationId: getTenant
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/TenantProfile'
                    description: Active tenant profile
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    $ref: '#/components/responses/Forbidden'
            summary: Profile of the session's active tenant
        patch:
            operationId: updateTenant
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/UpdateTenantRequest'
                required: true
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/TenantProfile'
                    description: Updated tenant profile
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    $ref: '#/components/responses/Forbidden'
                "422":
                    $ref: '#/components/responses/ValidationError'
            summary: Update tenant profile (owner only)
    /tenant/invites:
        get:
            operationId: listInvites
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                items:
                                    $ref: '#/components/schemas/Invite'
                                type: array
                    description: Open invites
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    $ref: '#/components/responses/Forbidden'
            summary: List open invites (owner or admin)
        post:
            operationId: createInvite
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/CreateInviteRequest'
                required: true
            responses:
                "201":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Invite'
                    description: Invite created
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    $ref: '#/components/responses/Forbidden'
            summary: Create a shareable invite link (owner or admin)
    /tenant/invites/{inviteId}:
        delete:
            operationId: revokeInvite
            responses:
                "204":
                    description: Invite revoked
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    $ref: '#/components/responses/Forbidden'
            summary: Revoke an invite (owner or admin)
        parameters:
            - in: path
              name: inviteId
              required: true
              schema:
                format: uuid
                type: string
    /tenant/members:
        get:
            operationId: listMembers
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                items:
                                    $ref: '#/components/schemas/Member'
                                type: array
                    description: Members of the active tenant
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    $ref: '#/components/responses/Forbidden'
            summary: List tenant members
    /tenant/members/{userId}:
        delete:
            operationId: removeMember
            responses:
                "204":
                    description: Member removed
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    $ref: '#/components/responses/Forbidden'
                "409":
                    $ref: '#/components/responses/Conflict'
            summary: Remove a member (owner only)
        parameters:
            - in: path
              name: userId
              required: true
              schema:
                format: uuid
                type: string
        patch:
            operationId: updateMember
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/UpdateMemberRequest'
                required: true
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Member'
                    description: Updated member
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "403":
                    $ref: '#/components/responses/Forbidden'
                "409":
                    $ref: '#/components/responses/Conflict'
            summary: Change a member's role (owner only)
    /tenants:
        post:
            operationId: createTenant
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/CreateTenantRequest'
                required: true
            responses:
                "201":
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/TenantProfile'
                    description: Tenant created and activated for this session
                "401":
                    $ref: '#/components/responses/Unauthorized'
                "422":
                    $ref: '#/components/responses/ValidationError'
            summary: Onboard a new tenant with its first warehouse; creator becomes owner
security:
    - sessionCookie: []
servers:
    - url: /v1

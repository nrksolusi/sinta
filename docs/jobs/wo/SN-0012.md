---
id: SN-0012
title: fix-3 Slice 1: draft delete, lifecycle actors, opname systemQty
type: FEAT
status: done
epic: fix-3
plan: ../../plans/fix-3-lifecycle-and-slice-a.md
blocked_by: -
lane: Joint
size: M
risk: Medium
priority: P1
branch: feat/fix-3-slice-1
created: 2026-07-23
updated: 2026-07-23
---

## Objective

Deliver Slice 1 of fix-3: allow draft documents to be deleted (INC-1), persist
who created and posted each document (INC-2), and snapshot the system qty on each
opname line at posting time (INC-3).

## References

- `../../plans/fix-3-lifecycle-and-slice-a.md` - plan this executes (Slice 1)
- `../../adr/0001-append-only-movement-journal.md` - opname movements are append-only
- `../../adr/0003-negative-stock-provisional-cost.md` - corrections are new entries, not updates
- `../../adr/0013-draft-deletion-immutability-exception.md` - draft is the one deletable state

## Scope fence

**In scope** - draft DELETE (204/409) for all 7 document types via cascade on
header row; `posted_by uuid` column on all 7 document tables (migration 0301);
`system_qty numeric` column on `stock_opname_lines` (migration 0302); `createdAt`,
`createdBy`, `postedAt`, `postedBy` on all 7 OpenAPI document schemas;
`DocumentActor` schema; `SetStockOpnameLineSystemQty` query; client `api-types.ts`
regenerated; client test fixtures updated with required `createdAt`/`createdBy`.

**Out of scope** - approval gate (Slice 2), surat jalan printing / numbering UI
(Slice 3), client-side delete buttons, timeline actor rendering, opname systemQty
display - those are follow-on jobs.

## Acceptance gate

- [x] DELETE /xxx/{id} returns 204 on draft, 409 on posted, 404 on unknown
- [x] GET /xxx/{id} returns createdAt, createdBy, postedAt (nullable), postedBy (nullable)
- [x] POST /xxx/{id}/post persists postedBy = calling user
- [x] Opname line systemQty populated at posting; reversal does not overwrite it
- [x] `go test ./...` green
- [x] `pnpm typecheck && pnpm test && pnpm lint` green (1 pre-existing timeout unrelated)

## Log

### 2026-07-23 · done
Spec-first: added `DocumentActor` schema, `createdAt`/`createdBy`/`postedAt`/`postedBy`
to all 7 document schemas, `systemQty` to `StockOpnameLine`, delete operations on
all 7 document paths. Ran `go run ./cmd/bundle` + oapi-codegen to regenerate
`api.gen.go`. Migrations 0301 (posted_by column) and 0302 (system_qty column)
written and applied. sqlc queries: 7 Delete* :exec queries, updated Mark*Posted to
set `posted_by`, added `SetStockOpnameLineSystemQty`. Handler layer: `docActors`
struct + `loadDocActors` helper in `documents.go`; all 7 handlers updated to load
and return actors; all 7 Mark*Posted call sites pass `PostedBy`; `opnameMovements`
persists `system_qty` when `!negate`; `DeleteStockOpname` handler as reference,
remaining 6 delete handlers written by subagent. Server tests: `TestDraftDeleteStockOpname`,
`TestDraftDeleteAllDocumentTypes`, `TestLifecycleTimestampsAndActor`,
`TestOpnameSystemQty` - all green. Client: `pnpm generate-api` updated `api-types.ts`;
test fixtures in 9 files updated with required `createdAt`/`createdBy` fields.
All gates green.

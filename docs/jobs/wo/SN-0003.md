---
id: SN-0003
title: Read-query + search (client)
type: FEAT
status: in-progress
epic: fix-4
plan: ../../plans/fix-4-ui-ux-revamp.md
blocked_by: SN-0002
lane: FE
size: M
risk: Low
priority: P1
branch: feat/sn-0003-read-query-client
created: 2026-07-23
updated: 2026-07-23
---

## Objective

Make document lists and entity pickers drive the server params from SN-0002, so
filtering/pagination/search happen server-side instead of in the browser.

## Required reading (weighted)

- **Must:** ADR-0019; `../../plans/fix-4-ui-ux-revamp.md` Slice 1 + Ground-truth on
  `DocList`, `combobox.tsx`/`product-combobox.tsx`/`pickers-data.ts`, the
  `onSearch` seam.
- **Should:** `hooks/use-mobile.ts` (Sheet swap for the picker on mobile).

## Scope fence

**In scope** - `DocList` consumers pass filters/cursor to the server via the
existing URL search state; drop in-memory full-list filtering. Rebuild pickers as
a Command **dialog** (Sheet on mobile) driving server `q` through `onSearch`;
preserve recents.

**Out of scope** - `ui/tabs.tsx` + tab presets + filter bar (SN-0008); any server
change. Consume only fields SN-0001/02 provide - if one is missing, STOP.

## Acceptance gate

- [ ] `DocList` consumers server-page and server-filter; no in-memory full-list filtering remains
- [ ] Pickers are a Command dialog (Sheet on mobile) driving server `q`; recents preserved
- [ ] A large list paginates without loading the whole set
- [ ] Tests cover the list-params wiring and picker search (test-first)
- [ ] `pnpm typecheck && pnpm test && pnpm lint` green; `generate-i18n` clean if messages changed; board exit gate satisfied

## Log

### 2026-07-23 · filed
Migrated from fix-4 work order (was FIX4-03). Blocked by SN-0002. Gates and scope fence above are the handoff contract.

### 2026-07-23 · unblocked
SN-0002 done; all blockers cleared.

### 2026-07-23 · start
Worktree feat/sn-0003-read-query-client created. Running parallel with SN-0004.

### 2026-07-23 · impl: list routes switched to useInfiniteQuery + PickerDialog + message keys

Implemented Phase 1 of SN-0003 (document list pagination and server-side filtering):

- Created `client/src/lib/doc-list-params.ts` (`buildDocListParams`): maps `DocListFilters + cursor` to the query params the 6 list endpoints accept. Tests first in `doc-list-params.test.ts` (6 cases, all green).
- Created `client/src/lib/picker-dialog.tsx` (`PickerDialog`): responsive trigger button that opens a Dialog on desktop or Sheet on mobile (via `useIsMobile`). Tests first in `picker-dialog.test.tsx` (4 cases, all green). Added `window.matchMedia` stub to `test-setup.ts` for jsdom compatibility.
- Switched all 7 document list routes from `useQuery` to `useInfiniteQuery`: receipts, purchase orders, sales orders, deliveries, stock transfers, stock adjustments, opnames.
- Each list now passes URL filters and cursor to the server; a "Load more" button appears when `hasNextPage` is true.
- Dead client-side filter calls (`sortReceipts`, `sortPurchaseOrders`, `filterSalesOrders`, `filterDeliveries`, `filterTransferRows`) removed from route components; functions kept in `-data.ts` files because they are tested independently and remain exported.
- Added 4 message keys to `id.json` + `en.json`: `doclist_load_more`, `picker_select_product`, `picker_select_partner`, `picker_select_warehouse`. Ran `generate-i18n`.
- Gate: `pnpm typecheck && pnpm test --run && pnpm lint` all green (208 tests pass, lint exit 0).

Still open: picker dialog internals (Command + onSearch + recents) - Phase 2.
